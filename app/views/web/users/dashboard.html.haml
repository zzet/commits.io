.row-fluid
  - if @commits.any?
    .b-dots
      = render 'web/shared/commits', commits: @commits
  - else
    .span12.center
      = link_to t('.Activate some of your repositories'), settings_repositories_path, class: 'btn btn-large btn-success'
